# Class 07 - Passing data to our app: GET method
What is a query string?

How the GET channel works in the request

##

http://<direccion>archivo.php?variable=valor&variable2=valor2

variables are concatenated with &

##

Examples:

- greeting.php

- multiplication.php

##

The function isset(<variable>) = returns `true` if `<variable exists>`, `fals`e otherwise.

Access the GET channel using the Server variable `$_GET`

`$valor=$_GET["variable"]`

## ⌨️ practical work  
Improve the rock paper scissors game so the computer (server) can play too 😃


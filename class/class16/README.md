# Class 16 - Structures and Decisions
Practicing with structures:

- Applying Simpson's rule

- Applying notes

Serialize/unserialize

Prepare a structure to store on disk, retrieve its original array form.

Funciones integradas:

`trim()`

`strtolower()` / `strtoupper()`

`count()`

`isset()`

`array_unshift()`

Practical Work: Application of expenses
